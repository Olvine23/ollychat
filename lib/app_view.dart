import 'package:flutter/material.dart';

class OllyChat extends StatelessWidget {
  const OllyChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Scaffold(
      body: Center(child: Text("OllyChat works!!!"),),
    );
  }
}