name: post_repository
description: "A package that handles posts."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.0.0
  firebase_core:
  firebase_storage:
  onesignal_flutter: ^5.1.2
  http:
  
  

  cupertino_icons: ^1.0.2
 
  cloud_firestore:
  user_repository:
    path: ../user_repository 
   

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
